% Generated by roxygen2 (4.1.0.9001): do not edit by hand
% Please edit documentation in R/re_functions2.r
\name{set.outgroups}
\alias{set.outgroups}
\title{Sets columns of outgroup individuals}
\usage{
set.outgroups(data, ploidy = 2)
}
\arguments{
\item{data}{output from check missing}

\item{ploidy}{the number of copies per individual}
}
\value{
An object with entries 'genotypes' and 'coords' containing the
   input data sets
}
\description{
As psi requires the derived state of an allele to be known, this function
provides an easy way to polarize SNP with outgroup individuals. From the
outgroup columns in the coord file, outgroup individuals are removed
and SNPs swithced if necessary
}
\examples{
\dontrun{
f = system.file("examples/example.data.bed", package="rangeExpansion")
c = system.file("examples/example.coordinates.csv", package="rangeExpansion")
coord.data <- load.coord.file(c, sep=",")
snp.data <- load.plink.file(f, sep=",")
raw.data <- check.missing(snp.data, coord.data)
raw.data <- set.outgroups(raw.data)
}
}

